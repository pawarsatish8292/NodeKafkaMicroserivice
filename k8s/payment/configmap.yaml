apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-config
data:
  PORT: "3000"
  NODE_ENV: "production"
  SERVICE_NAME: "payment-service"
  DB_HOST: "host.docker.internal"
  DB_USER: "root"
  DB_NAME: "payment_db"
  KAFKA_BROKER: "kafka:9092"